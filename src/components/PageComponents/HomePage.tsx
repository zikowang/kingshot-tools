/** @format */

import ReactLayout from "@/layouts/ReactLayout";
import { Text } from "@chakra-ui/react";

const HomePage = () => {
    return (
        <ReactLayout>
            <Text>Home</Text>
        </ReactLayout>
    );
};

export default HomePage;
